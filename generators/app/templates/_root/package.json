{
  "name": "<%= name.replace(/\s+/g, '-').toLowerCase() %>",
  "version": "0.0.0",
  "private": true,
  "description": "<%= description %>",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "gulp watch",
    "build": "gulp build",
    "lint": "gulp lint"
  }
}
