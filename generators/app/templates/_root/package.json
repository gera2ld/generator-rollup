{
  "name": "",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "gulp dev",
    "prebuild": "yarn lint && gulp clean",
    "build": "cross-env NODE_ENV=production gulp build",
    "lint": "eslint ."
  },
  "husky": {
    "hooks": {
      "pre-push": "yarn lint"
    }
  }
}
